<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="winter.back.main.mapper.UserMapper">

    <insert id="signup" parameterType="UserDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (email,password,name)
            values (#{email},#{password},#{name})
    </insert>

    <select id="finduserinfo" resultType="winter.back.main.dto.UserInfoDTO">
        SELECT id, email, name FROM user WHERE email = #{email}
    </select>
</mapper>
